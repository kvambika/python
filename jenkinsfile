pipeline {
    agent any

    stages {
        stage('version') {
            steps {
                sh 'python --version'
                echo 'python version'
            }
        }
    stage('build') {
            steps {
                sh 'python hello.py'
            }
        }
    }
}
